#jquery --JQuery框架
#modernizr --让不支持HTML5标签的浏览器识别<aside><section>等标签，另外，其主要功能是浏览器“功能检测”。
#js-webshim --基于modernizr，让各种浏览器都能显示HTML5的form表单样式。
#respond-master --让低版本IE支持max-width/min-width等属性
#html5-form-shim --基于modernizr，让各种浏览器都能显示HTML5的form表单样式。

#html5shiv --让低版本IE支持html5标签
#mediaqueries --让低版本IE支持media queries